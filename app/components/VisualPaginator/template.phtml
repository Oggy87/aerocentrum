{*
 * @version    $Id: template.phtml 2 2009-07-13 20:58:52Z david@grudl.com $
 * @param Paginator $paginator
 * @param array     $steps
 *}

{if $paginator->pageCount > 1}
<div class="paginator">
	{if $paginator->isFirst()}
	<span class="cufon button previous">« Předchozí</span>
	{else}
	<a class="cufon previous" href="{link this, 'page' => $paginator->page - 1}">« Předchozí</a>
	{/if}

	{foreach $steps as $step}
	{if $step == $paginator->page}
		<span class="cufon current">{$step}</span>
	{else}
		<a class="cufon" href="{link this, 'page' => $step}">{$step}</a>
	{/if}
	{if $iterator->nextValue > $step + 1}<span>…</span>{/if}
	{/foreach}

	{if $paginator->isLast()}
	<span class="cufon button next">Další »</span>
	{else}
	<a class="cufon next" href="{link this, 'page' => $paginator->page + 1}">Další »</a>
	{/if}
</div>
{/if}
